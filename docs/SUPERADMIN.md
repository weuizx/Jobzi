# Руководство суперадмина Jobzi

## Настройка суперадмина

### 1. Получите свой Telegram ID

Напишите боту [@userinfobot](https://t.me/userinfobot) и он покажет ваш Telegram ID.

### 2. Добавьте себя в базу данных как суперадмина

Подключитесь к базе данных PostgreSQL:

```bash
docker exec -it jobzi-postgres psql -U jobzi -d jobzi
```

Выполните SQL запрос для добавления суперадмина:

```sql
-- Вставить нового суперадмина
INSERT INTO users (telegram_id, first_name, username, role, is_active)
VALUES (123456789, 'Ваше имя', 'your_username', 'SUPERADMIN', true)
ON CONFLICT (telegram_id) DO UPDATE
SET role = 'SUPERADMIN';
```

Замените `123456789` на ваш реальный Telegram ID.

### 3. Проверка суперадмина

Откройте бота в Telegram и напишите `/start`. Вы должны увидеть сообщение с командами суперадмина.

---

## Настройка бота

### Установка переменных окружения

Создайте `.env` файл или установите переменные окружения:

```bash
export TELEGRAM_BOT_TOKEN="your-bot-token-from-botfather"
export TELEGRAM_BOT_USERNAME="your_bot_username"
```

Для получения токена:
1. Найдите в Telegram [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

---

## Команды суперадмина

### 1. Активация нового бизнеса

```
/superadmin activate @username "Название бизнеса"
```

**Пример:**
```
/superadmin activate @ivan_petrov "Строительная бригада Мастер"
```

**С описанием:**
```
/superadmin activate @ivan_petrov "Строительная бригада Мастер" "Строительство и ремонт"
```

**Что происходит:**
1. Бот запомнит, что для `@ivan_petrov` нужно создать бизнес
2. Когда пользователь `@ivan_petrov` напишет боту `/start`:
   - Создастся запись в таблице `users`
   - Создастся запись в таблице `businesses`
   - Создастся связь в `business_users` с ролью ADMIN
   - Пользователь получит доступ к функционалу бота

**Важно:** Попросите клиента написать боту `/start` после активации.

---

### 2. Просмотр всех бизнесов

```
/superadmin list
```

**Пример ответа:**
```
📋 Активные бизнесы (3):

1. 🟢 АКТИВЕН • ID: 1
   🏢 Строительная бригада Мастер
   👤 @ivan_petrov (987654321)
   📅 15.01.2026 14:30

2. 🟢 АКТИВЕН • ID: 2
   🏢 Фермерское хозяйство Урожай
   👤 @maria_farm (123456789)
   📅 10.01.2026 09:15

3. 🔴 ЗАБЛОКИРОВАН • ID: 3
   🏢 Грузоперевозки Быстро
   👤 @alex_cargo (555444333)
   📅 05.01.2026 18:00
```

---

### 3. Блокировка бизнеса

```
/superadmin block <business_id> <причина>
```

**Пример:**
```
/superadmin block 5 Неуплата
```

**Что происходит:**
- Поле `is_active` в таблице `businesses` устанавливается в `false`
- Клиент больше не может создавать вакансии и работать с откликами
- Существующие вакансии остаются, но их нельзя редактировать

**Ответ бота:**
```
🚫 Бизнес заблокирован

🆔 ID: 5
📝 Причина: Неуплата
📅 Дата блокировки: 20.01.2026 15:30
```

---

### 4. Разблокировка бизнеса

```
/superadmin unblock <business_id>
```

**Пример:**
```
/superadmin unblock 5
```

**Что происходит:**
- Поле `is_active` в таблице `businesses` устанавливается в `true`
- Клиент снова получает доступ ко всему функционалу

**Ответ бота:**
```
✅ Бизнес разблокирован

🆔 ID: 5
📅 Дата разблокировки: 25.01.2026 10:15
```

---

## Прямое управление через БД

### Добавить нового суперадмина

```sql
-- Если пользователь еще не существует
INSERT INTO users (telegram_id, first_name, username, role)
VALUES (987654321, 'Новый', 'new_admin', 'SUPERADMIN');

-- Если пользователь уже существует
UPDATE users
SET role = 'SUPERADMIN'
WHERE telegram_id = 987654321;
```

### Удалить права суперадмина

```sql
UPDATE users
SET role = 'USER'
WHERE telegram_id = 987654321;
```

### Просмотреть всех суперадминов

```sql
SELECT
    id,
    telegram_id,
    first_name,
    username,
    created_at
FROM users
WHERE role = 'SUPERADMIN';
```

### Просмотреть все бизнесы

```sql
SELECT
    b.id,
    b.name,
    u.username as owner_username,
    b.is_active,
    b.created_at
FROM businesses b
JOIN business_users bu ON bu.business_id = b.id AND bu.role = 'ADMIN'
JOIN users u ON u.id = bu.user_id
ORDER BY b.created_at DESC;
```

### Заблокировать бизнес вручную

```sql
UPDATE businesses
SET
    is_active = false,
    updated_at = NOW()
WHERE id = 5;
```

### Разблокировать бизнес вручную

```sql
UPDATE businesses
SET
    is_active = true,
    updated_at = NOW()
WHERE id = 5;
```

### Удалить бизнес полностью

```sql
-- ВНИМАНИЕ: Удалит бизнес и все связанные данные (вакансии, отклики)
DELETE FROM businesses WHERE id = 5;
```

---

## Workflow активации клиента

### Сценарий 1: Клиент оплатил, у вас есть его Telegram username

1. Клиент пишет вам: "Оплатил подписку"
2. Вы спрашиваете: "Как называется ваш бизнес?"
3. Клиент отвечает: "Строительная бригада Мастер"
4. Вы открываете бота и пишете:
   ```
   /superadmin activate @client_username "Строительная бригада Мастер"
   ```
5. Вы отправляете клиенту:
   ```
   ✅ Ваш аккаунт активирован!
   Перейдите в бота: @jobzi_bot
   Напишите команду /start
   ```
6. Клиент пишет `/start` боту → автоматически создается бизнес и он становится админом

### Сценарий 2: Клиент еще не написал боту

1. Клиент оплатил, но не использует Telegram активно
2. Вы получаете его Telegram ID (например, через платежную систему)
3. Вы подключаетесь к БД и создаете бизнес вручную:
   ```sql
   -- Создать пользователя
   INSERT INTO users (telegram_id, role)
   VALUES (987654321, 'USER')
   RETURNING id;

   -- Создать бизнес
   INSERT INTO businesses (name, telegram_chat_id)
   VALUES ('Строительная бригада', 987654321)
   RETURNING id;

   -- Связать пользователя с бизнесом как ADMIN
   INSERT INTO business_users (business_id, user_id, role)
   VALUES (1, 1, 'ADMIN');
   ```
4. Когда клиент напишет `/start`, он сразу увидит свой бизнес

---

## Troubleshooting

### Команды не работают

**Проблема:** Пишу `/superadmin list`, бот отвечает "⛔ У вас нет прав"

**Решение:**
1. Проверьте свой Telegram ID: напишите [@userinfobot](https://t.me/userinfobot)
2. Проверьте роль в БД:
   ```sql
   SELECT telegram_id, role FROM users WHERE telegram_id = YOUR_ID;
   ```
3. Если роль не SUPERADMIN, обновите:
   ```sql
   UPDATE users SET role = 'SUPERADMIN' WHERE telegram_id = YOUR_ID;
   ```

### Бот не отвечает

**Проблема:** Бот не реагирует на команды

**Решение:**
1. Проверьте, что бот запущен:
   ```bash
   docker logs jobzi-app
   ```
2. Проверьте токен в переменных окружения:
   ```bash
   echo $TELEGRAM_BOT_TOKEN
   ```
3. Проверьте логи приложения на ошибки

### Пользователь не может написать боту

**Проблема:** Клиент говорит "Не могу найти бота"

**Решение:**
1. Убедитесь, что бот создан через [@BotFather](https://t.me/BotFather)
2. Проверьте настройки бота: он должен быть публичным
3. Отправьте клиенту прямую ссылку: `https://t.me/your_bot_username`

---

## Безопасность

### Защита токена бота

**Не храните токен в коде!** Используйте переменные окружения:

```bash
# .env файл (добавьте в .gitignore!)
TELEGRAM_BOT_TOKEN=your-token-here
TELEGRAM_BOT_USERNAME=your_bot
```

### Защита БД

1. Используйте сильный пароль для PostgreSQL
2. Не открывайте порт 5432 наружу
3. Регулярно делайте бэкапы

### Управление суперадминами

1. Минимизируйте количество суперадминов
2. Логируйте все действия суперадминов
3. Периодически проверяйте список:
   ```sql
   SELECT * FROM users WHERE role = 'SUPERADMIN';
   ```

---

## Лог действий суперадмина

Для будущей реализации: добавить таблицу `admin_actions`:

```sql
CREATE TABLE admin_actions (
    id BIGSERIAL PRIMARY KEY,
    admin_user_id BIGINT REFERENCES users(id),
    action_type VARCHAR(50) NOT NULL,
    target_business_id BIGINT REFERENCES businesses(id),
    details JSONB,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

Это позволит отслеживать:
- Кто и когда активировал бизнесы
- Кто и почему блокировал
- История всех админских действий