# TODO - Задачи на следующую сессию

## Срочные баги для исправления

Все баги исправлены! ✅

## Новые функции

✅ **[2026-01-21]** Добавлен функционал удаления вакансии для бизнеса
✅ **[2026-01-21]** Проведен анализ интерфейса соискателя - предложено 10 улучшений UX
✅ **[2026-01-21]** Подготовлен детальный план реализации системы рассылки рекламных сообщений
✅ **[2026-01-21]** Реализована защита от потери контекста ответов при изменении вопросов

## Уже исправленные баги

✅ **[2026-01-21]** При добавлении вопроса в существующую вакансию создается новая вакансия
✅ **[2026-01-21]** Дата публикации не проставляется при создании вакансии
✅ **[2026-01-21]** После просмотра откликов из вакансии, кнопка "Назад" возвращает ко всем откликам
✅ **[2026-01-21]** Когда на вакансию нет откликов, кнопки интерфейса не работают
✅ **[2026-01-21]** В интерфейсе откликов вакансии не отображаются кнопки для навигации

### Описание исправлений от 2026-01-21

**1. Добавление вопроса в существующую вакансию:**
- Установлен флаг `editingExistingVacancy = true` при добавлении вопроса из управления анкетой
- Сохраняются данные вакансии (код, название, статус) для корректного возврата
- Файл: `BusinessTelegramService.kt:1910-1925`

**2. Дата публикации при создании вакансии:**
- Автоматическая установка `publishedAt` для вакансий со статусом ACTIVE
- Файл: `VacancyDbService.kt:48`

**3. Возврат из откликов к конкретной вакансии:**
- Добавлено новое состояние `VIEWING_VACANCY_APPLICATIONS`
- Сохраняется контекст вакансии (`viewingVacancyId`, `fromVacancyApplications`)
- Кнопка "Назад" из детального просмотра отклика возвращает к откликам конкретной вакансии
- Кнопка "Назад" из списка откликов возвращает к интерфейсу вакансии
- Добавлена валидация принадлежности отклика к вакансии
- Файлы: `ConversationState.kt:31`, `BusinessTelegramService.kt:127,1560,1806-1852`

**4. Интерфейс откликов без откликов:**
- При отсутствии откликов остается состояние `VIEWING_VACANCY_APPLICATIONS`
- Отображается информационное сообщение о возможности ввода ID
- Можно ввести ID отклика или нажать "Назад" для возврата к вакансии
- Файл: `BusinessTelegramService.kt:1732-1748`

**5. Кнопки в интерфейсе откликов:**
- Добавлена клавиатура с кнопкой "◀️ Назад" для состояния `VIEWING_VACANCY_APPLICATIONS`
- Кнопка отображается внизу экрана в Telegram и доступна для нажатия
- Файл: `JobziBot.kt:209-212`

---

### Новая функция от 2026-01-21

**Функционал удаления вакансии:**
- Добавлен метод `deleteVacancy(vacancyId: Long)` в `VacancyDbService`
- Добавлено состояние `VACANCY_DELETE_CONFIRM` для подтверждения удаления
- Добавлен обработчик в `BusinessTelegramService.handleVacancyAction` для кнопки "🗑 Удалить"
- Добавлена функция `handleVacancyDeleteConfirm` для обработки подтверждения
- Добавлена кнопка "🗑 Удалить" в интерфейс управления вакансией
- Добавлена клавиатура подтверждения с кнопками "Да, удалить" и "❌ Отмена"
- При удалении также удаляются все связанные вопросы и отклики (cascade)
- После удаления пользователь возвращается к списку вакансий

**Файлы:**
- `VacancyDbService.kt:124-132` - метод удаления
- `ConversationState.kt:55` - новое состояние
- `BusinessTelegramService.kt:142,1565-1597,1763-1820` - обработчики
- `KeyboardFactory.kt:253-296` - кнопки и клавиатура подтверждения
- `JobziBot.kt:188-191` - клавиатура для состояния

---

### Анализ интерфейса соискателя от 2026-01-21

**Проведен детальный анализ** `ApplicantTelegramService.kt` и выявлено 10 улучшений UX:

1. **Добавить этап подтверждения перед анкетой** - показывать полную информацию о вакансии с кнопками Да/Нет
2. **Динамические кнопки для вопросов CHOICE** - создавать клавиатуру с вариантами ответов (если ≤6)
3. **Возможность вернуться к предыдущему вопросу** - кнопка "◀️ Назад" для исправления ответов
4. **Сводка ответов перед отправкой** - финальный preview с возможностью редактирования
5. **Русификация статусов в /my** - заменить "NEW", "VIEWED" на "Новый", "Просмотрен"
6. **Визуальный индикатор прогресса** - добавить `[█░░░░] 20%` вместо только "1/5"
7. **Просмотр деталей своего отклика** - возможность посмотреть свои ответы на анкету
8. **Улучшение валидации телефона** - ужесточить regex, запретить "-------"
9. **Inline-подсказки форматов** - показывать примеры: "+7 999 123-45-67"
10. **Ограничение длины текста** - максимум 1000 символов для TEXT вопросов

**Файл:** `ApplicantTelegramService.kt` (398 строк)

---

### План системы рассылки рекламы от 2026-01-21

**Подготовлен детальный план** реализации системы конфигурации и рассылки рекламных сообщений:

**База данных (3 таблицы):**
- `advertising_campaigns` - рекламные кампании (текст, медиа, статус, расписание)
- `telegram_channels` - список каналов для рассылки (ID, название, активность)
- `broadcast_logs` - журнал отправок (статус, ошибки, статистика)

**Архитектура:**
- Domain entities: `AdvertisingCampaign`, `TelegramChannel`, `BroadcastLog` + enums
- Services: `CampaignDbService`, `ChannelDbService`, `BroadcastService`, `SuperAdminBroadcastService`
- Scheduler: `BroadcastScheduler` для отложенных рассылок
- States: 8 новых состояний диалога для создания кампаний и управления каналами

**Интерфейс суперадмина:**
- Меню рассылки с кнопками: Новая рассылка / Мои рассылки / Управление каналами
- Flow создания кампании (6 шагов): Название → Текст → Медиа → Preview → Выбор каналов → Подтверждение
- Управление каналами: добавление, редактирование, проверка прав бота
- Просмотр статистики: успешно отправленные, ошибки, охват

**Ключевые features:**
- Поддержка Markdown форматирования и медиа (фото, видео, документы)
- Планирование рассылок на будущее (scheduled broadcasts)
- Rate limiting с обработкой "Too Many Requests"
- Проверка прав бота в каналах перед отправкой
- Подробное логирование всех отправок
- Защита: только суперадмин, двойное подтверждение, audit log

**Этапы реализации:**
1. База данных и entities (2-3 ч)
2. Database services (2 ч)
3. BroadcastService - логика рассылки (3-4 ч)
4. Интерфейс суперадмина (4-5 ч)
5. Планировщик (1-2 ч)
6. Статистика и аналитика (2 ч)
7. Тестирование (2-3 ч)

**Итого:** 18-22 часа разработки

**Дополнительные features (опционально):**
- Шаблоны сообщений с переменными
- A/B тестирование
- Повторяющиеся рассылки (cron)
- Webhook для статистики просмотров
- Экспорт статистики в CSV/Excel

---

### Защита от потери контекста ответов от 2026-01-21

**Проблема:** При изменении или удалении вопросов в анкете вакансии терялся контекст ответов соискателей - было невозможно понять, на какой вопрос был дан ответ.

**Решение:** Реализован механизм snapshot (снимка) вопроса на момент отклика через денормализацию данных.

**Что изменено:**

**1. Миграция БД (011-add-question-context-to-answers.sql):**
```sql
ALTER TABLE answers
    ADD COLUMN question_text TEXT NOT NULL,
    ADD COLUMN question_type VARCHAR(20) NOT NULL,
    ADD COLUMN question_order INTEGER NOT NULL;
```

**2. Entity Answer (Answer.kt:29-36):**
- Добавлены поля `questionText`, `questionType`, `questionOrder`
- Сохраняют snapshot вопроса на момент отклика
- Гарантируют сохранение контекста даже при удалении/изменении вопроса

**3. ApplicationDbService.saveAnswer (ApplicationDbService.kt:57-74):**
- Обновлена сигнатура метода для приема дополнительных параметров
- Теперь сохраняет полный контекст вопроса вместе с ответом

**4. ApplicantTelegramService (ApplicantTelegramService.kt:216-223):**
- При сохранении ответа передается текст, тип и порядковый номер вопроса
- Snapshot создается в момент отклика соискателя

**5. BusinessTelegramService (BusinessTelegramService.kt:1237-1281):**
- Убрана загрузка вопросов из таблицы `questions`
- Используется сохраненный текст из `answer.questionText`
- Ответы сортируются по `questionOrder` для правильного отображения

**Преимущества:**
- ✅ Ответы не теряют контекст при изменении вопросов
- ✅ История откликов остается корректной даже после удаления вопросов
- ✅ Бизнес видит ту же формулировку вопроса, на которую отвечал кандидат
- ✅ Простая реализация без сложного версионирования
- ✅ Обратная совместимость - миграция заполняет существующие записи

**Файлы:**
- `011-add-question-context-to-answers.sql` - миграция БД
- `011-add-question-context-to-answers.xml` - Liquibase changeset
- `db.changelog-master.xml:19` - добавлена новая миграция
- `Answer.kt:29-36` - новые поля entity
- `ApplicationDbService.kt:57-74` - обновленный метод saveAnswer
- `ApplicantTelegramService.kt:216-223` - сохранение snapshot при отклике
- `BusinessTelegramService.kt:1237-1281` - отображение ответов из snapshot

---

#### 1. При добавлении вопроса в существующую вакансию создается новая вакансия
**Проблема**: Когда пользователь открывает существующую вакансию и пытается добавить вопрос в анкету через "Анкета" → "Добавить", система создает НОВУЮ вакансию вместо добавления вопроса в существующую.

**Причина**: Не установлен флаг `editingExistingVacancy = true` при добавлении вопроса из управления анкетой.

**Где исправить**:
- Файл: `src/main/kotlin/dev/weuizx/jobzi/telegram/service/BusinessTelegramService.kt`
- Функция: `handleQuestionnaireManagementAction`
- Строки: ~1840-1852

**Что нужно сделать**:
```kotlin
action.contains("добавить") -> {
    val vacancyId = stateManager.getContextValue<Long>(message.userId, "currentVacancyId")
    if (vacancyId != null) {
        stateManager.setContextValue(message.userId, "vacancyId", vacancyId)
        // ДОБАВИТЬ:
        stateManager.setContextValue(message.userId, "editingExistingVacancy", true)

        // Сохранить код вакансии для возврата:
        val vacancy = vacancyDbService.findById(vacancyId)
        if (vacancy != null) {
            stateManager.setContextValue(message.userId, "vacancyCode", vacancy.code)
            stateManager.setContextValue(message.userId, "vacancyTitle", vacancy.title)
            stateManager.setContextValue(message.userId, "vacancyStatus", vacancy.status.name)
        }
    }
    // ...
}
```

---

### 2. Дата публикации не проставляется при создании вакансии
**Проблема**: При создании вакансии со статусом ACTIVE поле `publishedAt` остается NULL, хотя должно быть заполнено текущей датой.

**Где исправить**:
- Файл: `src/main/kotlin/dev/weuizx/jobzi/service/db/VacancyDbService.kt`
- Функция: `createVacancy`
- Строки: ~30-51

**Что нужно сделать**:
```kotlin
fun createVacancy(
    businessId: Long,
    title: String,
    description: String,
    location: String? = null,
    salary: String? = null,
    status: VacancyStatus = VacancyStatus.DRAFT
): Vacancy {
    val code = generateUniqueCode()

    val vacancy = Vacancy(
        businessId = businessId,
        code = code,
        title = title,
        description = description,
        location = location,
        salary = salary,
        status = status,
        // ДОБАВИТЬ:
        publishedAt = if (status == VacancyStatus.ACTIVE) OffsetDateTime.now() else null
    )

    return vacancyRepository.save(vacancy)
}
```

---

## Уже исправленные баги

✅ Исправлено добавление вопроса из управления анкетой - установить vacancyId
✅ Исправлен 'Заполнить заново' - возвращаться к просмотру вакансии
✅ Исправлено отображение заметок - показывать сразу после сохранения
✅ Улучшен UX создания вакансии - перенесен выбор статуса после анкеты

---

**Дата создания**: 2026-01-18