# ✅ Конфигурация для Локальной Разработки

## 🎯 Что Сделано

### 1. ✅ Создан `application-local.yml`
**Путь:** `src/main/resources/application-local.yml`

Этот файл содержит все настройки для локальной разработки.

### 2. ✅ Добавлен в `.gitignore`
Файл `application-local.yml` добавлен в `.gitignore`, чтобы секреты не попали в Git.

### 3. ✅ Создана Run Configuration
**Путь:** `.run/JobziApplication (local profile).run.xml`

IntelliJ IDEA автоматически подхватит эту конфигурацию.

### 4. ⚠️ Удалите `.env` вручную
Выполните в терминале:
```bash
rm /Users/a.kurtyakov/IdeaProjects/jobzi/.env
```

---

## 🚀 Как Использовать

### Шаг 1: Заполните Credentials

Откройте файл:
```
src/main/resources/application-local.yml
```

Заполните ваши данные:
```yaml
telegram:
  client:
    api-id: 12345678  # Ваш API ID с https://my.telegram.org
    api-hash: "your_api_hash_here"  # Ваш API Hash
    phone-number: "+1234567890"  # Ваш номер телефона
    session-path: "./telegram-session"
    enabled: true
```

### Шаг 2: Удалите `.env` (Если еще не удалили)

```bash
rm .env
```

### Шаг 3: Запустите в IntelliJ IDEA

#### Вариант А: Через Dropdown (Рекомендуется)

1. В правом верхнем углу IDEA найдите **dropdown с конфигурациями запуска**
2. Выберите **"JobziApplication (local profile)"**
3. Нажмите **зеленую кнопку ▶️ Run**

![Run Configuration](https://i.imgur.com/example.png)

#### Вариант Б: Через меню

1. `Run` → `Run...`
2. Выберите **"JobziApplication (local profile)"**
3. Приложение запустится с профилем `local`

---

## 📋 Что Произойдет

При запуске приложение:

1. **Прочитает** `application-local.yml` (вместо переменных окружения)
2. **Загрузит** все ваши credentials
3. **Запустит** TDLight с вашим аккаунтом
4. **Попросит ввести код** при первом запуске (в консоли IDEA)

### Первая Аутентификация

В консоли IDEA появится:
```
Insert the login code:
```

1. Откройте **Telegram** на телефоне
2. Получите **код**
3. **Введите** в консоль IDEA
4. Если есть 2FA - введите пароль

После успешной авторизации:
```
✅ LOGGED IN - Client is ready!
✅ Authenticated as: Ваше Имя (ID: ...)
```

---

## 🧪 Тестирование

После запуска приложения:

```bash
# Проверка статуса
curl http://localhost:8080/api/telegram-client/test/status

# Список чатов
curl http://localhost:8080/api/telegram-client/test/chats

# Отправка сообщения
curl -X POST http://localhost:8080/api/telegram-client/test/send \
  -H "Content-Type: application/json" \
  -d '{"chatId": -1001234567890, "message": "Тест!"}'
```

---

## 📁 Структура Файлов

```
jobzi/
├── .run/
│   └── JobziApplication (local profile).run.xml  ✅ Run Configuration
├── src/main/resources/
│   ├── application.yml                           ✅ Общие настройки
│   └── application-local.yml                     ✅ Локальные секреты (не в Git!)
├── .gitignore                                     ✅ Обновлен
└── .env                                           ❌ Удалите вручную!
```

---

## ⚠️ Важно!

### Безопасность

- ✅ `application-local.yml` **НЕ коммитится** в Git (в `.gitignore`)
- ✅ Храните credentials только локально
- ✅ Не делитесь файлом `application-local.yml`

### Если Нужно Несколько Профилей

Можно создать дополнительные профили:

```
application-dev.yml      # Для dev сервера
application-staging.yml  # Для staging
application-prod.yml     # Для продакшена
```

И запускать с разными профилями:
```bash
-Dspring.profiles.active=dev
-Dspring.profiles.active=staging
```

---

## 🎉 Готово!

Теперь вы можете:

1. ✅ Запускать приложение **прямо из IDEA** (без `./gradlew bootRun`)
2. ✅ Не вводить credentials каждый раз
3. ✅ Не хранить секреты в переменных окружения
4. ✅ Не беспокоиться о случайном коммите секретов

**Просто нажмите ▶️ в IDEA и всё работает!** 🚀

---

## 🐛 Решение Проблем

### "Profile 'local' not found"

**Решение:** Убедитесь что файл называется **точно** `application-local.yml`

### "api-id is 0"

**Решение:** Заполните credentials в `application-local.yml`

### "Can't read application-local.yml"

**Решение:** Файл должен быть в `src/main/resources/application-local.yml`

### Приложение не видит конфигурацию

**Решение:**
1. Перезапустите IDEA
2. `File` → `Invalidate Caches` → `Invalidate and Restart`
